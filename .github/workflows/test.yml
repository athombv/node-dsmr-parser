name: Test

on:
  workflow_dispatch:
  push:
    branches:
      - master
      - develop
      - production
      - testing
  pull_request:


jobs:
  test:
    name: Test
    uses: athombv/athom-github-workflow/.github/workflows/test.yml@master
    secrets: inherit